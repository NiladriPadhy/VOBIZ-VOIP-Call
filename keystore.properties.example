# Copy to keystore.properties (gitignored) or pass the same values as env vars.
# storeFile may be relative to the repository root or an absolute path.
storeFile=keystore/release.keystore
storePassword=replace-me
keyAlias=vobizvoip
keyPassword=replace-me
